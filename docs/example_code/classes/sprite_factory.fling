namespace[Sprite]
(
enum[InitArgs: u8]
(
	None,
	Copy,
	Move,
),

gen<func>[build_init_arg_types_map(T: type):
	ctmap
	<
		InitArgs,
		gen<tuple>
		(
			egg0(bool),
			no_result(bool),
			return_type(type)
		)
	>]
(
	var(ret: ret_t),
	var(spec(const), ts = tokstrm(T)),
	cond
	(
	),
),

class[Vec2<T: type>]
(
	//gen<scope>
	//(
	//	gen<var>(old_ts = tokstrm(T)),
	//	gen<var>(iat_map = gen<build_init_arg_types_map>(tokstrm(T))),
	//),
),
)
