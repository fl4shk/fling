#ifndef src_symbol_class_hpp
#define src_symbol_class_hpp

// src/symbol_class.hpp

#include "misc_includes.hpp"
#include "misc_types.hpp"
#include "general_allocator_class.hpp"


//#include "symbol_value_class.hpp"

namespace frost_hdl
{

class FrostProgramNode;

// This class is intentionally barebones.
class Symbol final
{
private:		// variables
	SavedString _ident = nullptr;
	FrostProgramNode* _decl_fpn = nullptr;

public:		// functions
	Symbol();

	Symbol(SavedString s_ident, FrostProgramNode* s_decl_fpn);

	GEN_MOVE_ONLY_CONSTRUCTORS_AND_ASSIGN(Symbol);

	~Symbol();

	GEN_GETTER_BY_VAL(ident)
	GEN_GETTER_BY_VAL(decl_fpn)
};

} // namespace frost_hdl

#endif		// src_symbol_class_hpp
